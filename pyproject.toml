[project]
name = "format-kit"
version = "0.2.1"
requires-python = ">=3.8"
dependencies = ["style-profile"]
[tool.pytest.ini_options]
testpaths = ["tests"]
[build-system]
requires = ["setuptools", "wheel", "style-profile"]
build-backend = "setuptools.build_meta"
